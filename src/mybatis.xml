<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
<!-- 配置别名 -->
<typeAliases>
	<!-- 批量扫描别名 -->
	<package name="com.zzzy.po"/>
</typeAliases>

<!-- 配置mapper映射文件 -->
<!-- 和spring整合后，使用mapper扫描器，这里不需要配置了
单必须 遵循一些规范：需要将mapper接口的类名和mapper.xml映射文件的名称保持一致，且在同一个目录下 -->
<!--<mappers></mappers>-->
</configuration>